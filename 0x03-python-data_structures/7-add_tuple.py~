#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    if len(tuple_a) > 2:
        tuple_a = tuple_a[:2]
    elif len(tuple_b) > 2:
11;rgb:0000/0000/0000tuple_b = tuple_b[:2]
    elif len(tuple_a) < 2:
        if tuple_a[0] == None:
            tuple_a[0] = 0
        #elif tuple_a[1] == None:
            tuple_a[1] = 0
    elif len(tuple_b) < 2:
        if tuple_b[0] == None:
            tuple_b[0] = 0
        #elif tuple_b[1] == None:
            tuple_b[1] = 0


    #alist = list(tuple_a)
    #blist = list(tuple_b)
    #int a, b
    #listaddition = ['a', 'b']
    #listaddition[0] = alist[0] + blist[0]
    #return listaddition
    #listaddition[1] = alist[1] + blist[1]

    addition = tuple_a[0] + tuple_b[0], tuple_a[1] + tuple_b[1]
    #addition = tuple(listaddition)

    return addition
    
    
        
