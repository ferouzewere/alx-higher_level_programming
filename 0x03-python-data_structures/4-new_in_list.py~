#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    original = my_list.copy()
    if idx < 0:
        return original
    elif idx >= len(my_list):
        return original
    else:
        my_list[idx] = element
        return my_list
